<div class="jumbotron col-md-12">
  <% if user_signed_in? %>
    <div class="jumbotron col-md-8 col-md-offset-2">
    <h1>Welcome back, trusted user!</h1>

    <p class="lead">
      You have signed in!  If this were a real app some sweet conent
      would go here.  But it's just a test.  You might want to sign out now.
    </p>
    <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger btn-responsive' %>
    </div>
  <% else %>
    <h1>Welcome, new user!</h1>
    <p class="lead">
      You must sign in or sign up before you can access our super sweet,
      top secret content.
    </p>
    <div class="row-fluid ">
      <div class="span6 pull-left">
        <%= link_to 'Sign In', new_user_session_path, :class => 'btn btn-success btn-responsive' %>
      </div>
      <div class="span6 pull-right">
        <%= link_to 'Sign Up', new_user_registration_path, :class => 'btn btn-success btn-responsive' %>
      </div>
    </div>
  <% end %>
</div>
